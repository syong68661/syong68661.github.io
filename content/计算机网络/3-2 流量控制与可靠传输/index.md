+++

title = '3.2 流量控制与可靠传输'

date = 2026-05-30T21:00:00+08:00

lastmod = 2026-05-30T21:00:00+08:00

draft = false

weight = 32

slug = "flow control and reliable transmission"

summary = "解决数据链路层帧差错的方案，实现可靠传输"

tags = []

categories = ["计算机网络"]

+++

  

**重点**
**易错**
**易混**
  

## 1. 本质
上一小节只能解决==比特差错==的问题，而无法解决==帧差错==的问题。所以在==帧编号、确认和重传机制==的基础上，产生**流量控制**和**可靠传输**。

## 2. 结构/组成
下面说的方案大致相同，不过一方面时从流量控制来看问题；一方面是从可靠传输来看问题。使用**信道利用率**和**有效传输速率**来进行比较。
### 信道利用率和有效传输速率
本质为计算`传送第一个数据的时间T1+RTT往返时延+确认帧的发送时间T2=一个发送周期的时间`。
![](<Pasted image 20260530213456.png>)
当一个发送周期计算出来后，要计算有效传输速率时，只需要知道在该周期内，传送的数据量就可以得出结果。
![](<Pasted image 20260530213716.png>)

### 流量控制
避免发送方的发送速率大于接收方的发送速率，导致接收方无法处理接收来的数据，从而造成数据大量丢失。所以通过以下方法来*控制发送方的发送速率*。
1. 停止等待协议
	相当于发送窗口为1，接收窗口为1。
	由信道利用率的计算公式可知，发送完一个数据后，必须要等待确认帧，从而其效率较低。
	![](<Pasted image 20260530213921.png>)
2. 滑动窗口流量控制：避免了等待时间，从而提高了效率。
	允许发送一个数据后，若其它数据在发送窗口内，则不用等待确认帧，可以继续发送该数据。即发送窗口限制了发送方可以发送的帧最大数量。
	![](<Pasted image 20260530214240.png>)
	同样有接收窗口，只接收窗口内的帧，对于窗口外的帧，则直接丢弃。
	![](<Pasted image 20260530214515.png>)
### 可靠传输机制
接收方收到数据后，需要向发送方发送对应的确认帧；发送方拥有超时重传，当在一个时间段内，没有收到对应帧的确认帧，则重新发送该帧。以上过程为==自动重传请求协议（ARQ）==。  
1. 停止等待协议
2. 后退N帧协议（GBN）
	![](<Pasted image 20260530215229.png>)
	设计了==累计确认==机制，不要求每一个帧都有确认帧。但是要求==接收方按序接收数据帧==。所以接收方的窗口大小为1。
	![](<Pasted image 20260530215257.png>)
	若使用n比特对数据帧编号，则帧序列从`0到2^n-1`，对于发送窗口要求`1到2^n-1`，从而避免把旧帧当新帧处理（新瓶装旧酒）。
	![](<Pasted image 20260530215637.png>)
3. 选择重传协议（SR)
	放弃了累计确认机制，要对每个帧进行==单独确认==。所以，接收方==不要求按序接收==，同时接收方的窗口不要求大小为1。
	当接收方检测出帧出错时，会向发送发发送否定确认帧（NAK）。
	![](<Pasted image 20260530215955.png>)
	若使用n比特来进行帧编号，则帧序列从`0到2^n-1`，对于发送窗口和接收窗口要求`发送窗口+接收窗口<=2^n`,通常接收窗口与发送窗口大小一样，即都为`2^(n-1)`，从而避免新瓶装旧酒，也避免了发送窗口大于接收窗口，导致接收方无法及时处理多余发送过来的数据。
	![](<Pasted image 20260531154423.png>)
### 连续ARQ协议的信道利用率
显然停止等待协议无法做到连续发送数据，所以考虑的是GBN和SR两种协议。关键在于在发送一个数据的周期内，发送方发送n个数据是否大于这个周期，若大于该周期，则信道利用率为100%。
![](<Pasted image 20260531154909.png>)
若在一个周期内，发送方发送了n个数据，则需要等待第一个数据的确认帧到达后，才可以继续发送。
![](<Pasted image 20260531155210.png>)

## 3. 核心操作
- 后续补充

  

---
  

## 4. 常考点
- 后续补充

  

---
  

## 5. 易错点
- 后续补充

  

---
  

## 6. 关联知识
- 后续补充

  

---
  

##  7. 真题记录
- 后续补充

  

---